/*
type ( @ 'u1 @ ) custom_type =
| C1 of int @ unique
| C2 of bool @ 'u1 * ( ; unique ; ) custom_type @ shared

{
  let x = C1 (42) in
  match x with
  | _ -> { (0, true) }
  | C1(y) -> { (y, true) }
  | C2(y, C1(_)) -> { (1, y) }
  endmatch
}
*/

type ('t1 @ 'u1 @ 'a1) custom_type =
| Atom

{
  let (x, y) = (0, Atom) in (x, x, y)
}
