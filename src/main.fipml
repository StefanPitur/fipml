type custom_type =
| C1 of int
| C2 of bool * custom_type

fip fun add (x : int) ^(y : int) : int = {
  x + y
}

fun wrapper_f ^(f : int -> int -> int) (x : int) (y : int) : int = {
  ^f (x, y)
}

{
  wrapper_f (add, 0, 1)
}
