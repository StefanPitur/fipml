/*
type ( @ 'u1 @ ) custom_type =
| C1 of int @ unique
| C2 of bool @ 'u1 * ( ; unique ; ) custom_type @ shared

{
  let x = C1 (42) in
  match x with
  | _ -> { (0, true) }
  | C1(y) -> { (y, true) }
  | C2(y, C1(_)) -> { (1, y) }
  endmatch
}
*/
{
  let (x, y) = (1, true) in let z = 3 in (x, y, y, z, z)
}
