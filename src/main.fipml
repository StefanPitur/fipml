type custom_type =
| C1 of int
| C2 of bool * custom_type

{
  let x = C1 (10) in
  let (y1, y2) =
    match x with
    | _ -> { (0, true) }
    | C1(y) -> { (y, true) }
    | C2(y, C1 (_)) -> { (1, y) }
    endmatch
  in y1
}
