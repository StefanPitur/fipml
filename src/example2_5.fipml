type list =
| Cons of list * list
| Nil

type boolean = 
| True
| False

fip(1) fun example2_5 ^(x : boolean) : list = {
  match x with
  | True -> { inst 2; Cons(Nil, Nil) }
  | False -> { weak 1; Nil }
  endmatch
}

{
  let x = True in example2_5 (x)
}
